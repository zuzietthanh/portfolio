{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Projects",
  "description": "A list of projects. The whole file is wrapped in [ ] and each project in { }.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["id", "title"],
    "additionalProperties": false,
    "properties": {
      "_comment": { "type": "string" },
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$",
        "description": "Web address for this project. Lowercase letters, numbers and hyphens only — no spaces. Must be different from every other project's id."
      },
      "title": { "type": "string", "minLength": 1 },
      "subtitle": { "type": "string", "description": "One line under the title." },
      "description": {
        "type": "string",
        "description": "The full write-up. Basic HTML allowed: <p>, <h2>, <ul>, <li>, <strong>."
      },
      "cover_image": {
        "type": "string",
        "description": "e.g. \"/images/my-project.jpg\". Leave \"\" for no image."
      },
      "tech_stack": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Tools and skills, shown as small pills."
      },
      "project_role": { "type": "string", "description": "Your role, e.g. \"Campaign Lead\"." },
      "github_url": { "type": "string" },
      "live_url": { "type": "string", "description": "Link to the live project. Leave \"\" to hide." },
      "featured": { "type": "boolean", "description": "true shows a \"Featured\" tag." },
      "sort_order": { "type": "number", "description": "Lower numbers appear first." },
      "is_published": { "type": "boolean", "description": "false hides it without deleting it." }
    }
  }
}
